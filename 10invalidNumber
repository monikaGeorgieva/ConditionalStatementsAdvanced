function solve(input) {
    let n = Number(input[0]);
    if (n < 100 && n != 0) {
     console.log("invalid")
    }else if (n > 200) {
     console.log("invalid")
    }
    
 }
solve(["75"]);
solve(["150"]);
solve(["220"]);
solve(["199"]);
solve(["-1"]);
solve(["100"]);
solve(["200"]);
solve(["0"]);
